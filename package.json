{
  "name": "@ran-sh/gpt-relay",
  "version": "2.5.0",
  "description": "GPT-first durable workflow relay with audited decisions and multi-agent executors",
  "type": "module",
  "bin": {
    "gpt-relay": "bin/gpt-relay.mjs",
    "agent-workflow": "bin/agent-workflow.mjs"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "test:coverage": "node --test --experimental-test-coverage --test-coverage-include=lib/**/*.mjs --test-coverage-exclude=lib/path-policy.mjs --test-coverage-lines=80 --test-coverage-functions=80 test/v2-*.test.mjs",
    "validate:examples": "node validator/validate-contract.mjs task examples/contracts/task-contract.example.json && node validator/validate-contract.mjs result examples/contracts/result-contract.example.json",
    "prepublishOnly": "npm test && npm run test:coverage && npm run validate:examples"
  },
  "files": [
    "bin/",
    "lib/",
    "templates/",
    "schema/",
    "validator/",
    "VERSION",
    "README.md"
  ],
  "engines": {
    "node": ">=24"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Ran-sh/gpt-relay.git"
  },
  "bugs": {
    "url": "https://github.com/Ran-sh/gpt-relay/issues"
  },
  "homepage": "https://github.com/Ran-sh/gpt-relay#readme",
  "keywords": ["agents", "workflow", "codex", "claude", "sqlite"],
  "publishConfig": {
    "access": "public"
  }
}
